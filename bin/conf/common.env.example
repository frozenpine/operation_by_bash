# data base dir
DATA_BASE=/data

# app base dir
APP_BASE=/opt

# SSH private key for authorization
# use user default key if blank.
IDENTITY_FILE="~/.ssh/Jisheng-func-test.pem"

# specify if remote user need to get privilege with sudo
# if not necessary, leave blank
SUDO="sudo"

# User name used in remote manage
# can be modified by `-u {user_name}` in `allssh` `allscp` command 
REMOTE_USER=

# database settings
DEFAULT_DB_USER=trader
DEFAULT_DB_PASS=js2018

TRADE_TMDB_DB_NAME=clear

DIGITAL_REST_DB_NAME="sso digital"
DIGITAL_SERVICEIMPL_DB_NAME=digital

SERVICE_INDEX_DB_NAME=digital
SERVICE_SMS_DB_NAME=sms

# container version definitions
DIGITAL_REST_VERSION=1.0.4-SNAPSHOT
DIGITAL_SERVICEIMPL_VERSION=1.0.4-SNAPSHOT

TRADE_ORDER_VERSION=1.0.1-SNAPSHOT
TRADE_CLEAR_VERSION=1.0.1-SNAPSHOT
TRADE_TMDB_VERSION=1.0.1-SNAPSHOT
TRADE_MATCH_VERSION=1.0.1-SNAPSHOT

SERVICE_INDEX_VERSION=1.0-SNAPSHOT
SERVICE_SMS_VERSION=1.0-SNAPSHOT
SERVICE_TRADEBASE_VERSION=1.0-SNAPSHOT